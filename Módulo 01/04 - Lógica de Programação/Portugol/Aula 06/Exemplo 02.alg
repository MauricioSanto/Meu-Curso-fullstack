Algoritmo "Vetor - Conjunto de Dados"

Var listaNumeros: vetor[0..2] de inteiro
    x: inteiro

Inicio

    listaNumeros[0] <- 10
    listaNumeros[1] <- 20
    listaNumeros[2] <- 30
    listaNumeros[3] <- 40

    escreval (listaNumeros)
    escreval ("----------")

    para x de 0 ate 3 faca
        escreval (listaNumeros[x])
    fimpara

Fimalgoritmo